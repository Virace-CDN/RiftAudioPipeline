[build-system]
requires = ["hatchling>=1.24.2"]
build-backend = "hatchling.build"

[project]
name = "rift-audio-pipeline"
version = "0.1.0"
description = "LOL 语音资源自动化流水线"
requires-python = ">=3.13"
dependencies = [
    "loguru==0.7.3",
    "python-dateutil==2.9.0.post0",
    "riotmanifest==2.4.0",
    "urllib3==2.6.3",
    "lol-audio-unpack @ git+https://github.com/Virace/lol_extract_voice.git@v3-test",
]

[project.scripts]
rift-baidu-oauth = "rift_audio_pipeline.baidu.cli:main"
rift-audio-pipeline = "rift_audio_pipeline.pipeline.cli:main"
rift-faker-github = "rift_audio_pipeline.control_plane.faker_github:main"
rift-control-plane-init = "rift_audio_pipeline.control_plane.runtime_init:main"
rift-control-plane-job = "rift_audio_pipeline.control_plane.job_runner:main"
rift-control-plane-log-worker = "rift_audio_pipeline.control_plane.log_relay:main"
rift-control-plane-upload-worker = "rift_audio_pipeline.control_plane.upload_worker:main"
rift-control-plane-finalize-worker = "rift_audio_pipeline.control_plane.finalize_worker:main"
rift-mock-control-plane = "rift_audio_pipeline.control_plane.mock_server:main"
rift-mock-smoke = "rift_audio_pipeline.control_plane.mock_smoke:main"
rift-control-plane-e2e = "rift_audio_pipeline.control_plane.e2e_simulation:main"

[dependency-groups]
dev = [
    "pytest==9.0.2",
]

[tool.hatch.build.targets.wheel]
packages = ["src/rift_audio_pipeline"]

[tool.hatch.metadata]
allow-direct-references = true

[tool.ruff]
target-version = "py311"
line-length = 100
exclude = [
    "third_party/baidu_sdk",
    "third_party/baidu_sdk/openapi_client",
]

[tool.ruff.format]
quote-style = "double"

[tool.pytest.ini_options]
markers = [
    "real_data: 需要联网并使用本地缓存的真实链路测试",
]
